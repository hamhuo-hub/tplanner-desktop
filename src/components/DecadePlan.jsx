function DecadePlan() {
    return (
        <div style={{
            flex: 1,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            color: 'var(--clr-text-dim)',
            fontFamily: 'var(--font-display)',
            fontSize: 14,
            letterSpacing: '0.1em',
            textTransform: 'uppercase',
        }}>
            十年计划 — 即将推出
        </div>
    );
}

export default DecadePlan;
